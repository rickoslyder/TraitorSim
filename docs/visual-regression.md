# Visual Regression

Summary: visual changes require screenshots and comparison via a real adapter. Configure `[commands].visual`, then run `python /home/hermes/repos/agent-operability-kit/bin/aok_visual.py .`.
