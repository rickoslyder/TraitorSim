# Periodic Sweeps

Summary: sweeps are read-only repo health passes across recent work. Findings create tasks; sweeps do not silently rewrite code.

Run: `python /home/hermes/repos/agent-operability-kit/bin/aok_sweep.py .`
